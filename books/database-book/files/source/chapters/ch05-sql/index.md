# Introduction

## Chapter 5: SQL — The Language of Databases

*How Structured Queries Transform Stored Data into Business Insight*

Chapter 4 introduced databases as structured systems for storing, protecting, and managing data. Chapter 5 introduces the language used to work with those systems: **SQL**, or **Structured Query Language**.

SQL is the standard language used to define database structures, insert records, retrieve results, and summarize patterns. It is one of the most transferable skills in business technology because the same core logic appears across Microsoft Access, SQLite, PostgreSQL, SQL Server, Oracle, and cloud data platforms.

This chapter treats SQL as a way to ask precise questions of structured data — not as a list of syntax to memorize. You will learn how to write queries that filter, sort, group, and join data, turning business questions into repeatable database statements.

## Chapter Roadmap

| Section | Main Question | Why It Matters |
| --- | --- | --- |
| [Part 1: SQL Foundations and Tools](#part-1-sql-foundations-and-tools) | What is SQL, and how does it work as a declarative language? | Establishes SQL as the universal language of relational databases — the same logic works across platforms. |
| [Part 2: The Chapter 5 Teaching Dataset](#part-2-the-chapter-5-teaching-dataset) | What data will we use to learn SQL? | Introduces the GRADEBOOK and GRADE_WEIGHT tables as a concrete, relatable dataset for building queries. |
| [Part 3: Creating Tables and Inserting Data](#part-3-creating-tables-and-inserting-data) | How do you build a database from scratch? | `CREATE TABLE` and `INSERT INTO` give you control over what data exists and how it is structured. |
| [Part 4: Querying Data with SELECT](#part-4-querying-data-with-select) | How do you retrieve and filter the data you need? | `SELECT`, `FROM`, `WHERE`, `ORDER BY`, and `DISTINCT` are the building blocks of every business report. |
| [Part 5: A First Look at Joins](#part-5-a-first-look-at-joins) | How do you combine information from two tables? | A preview of `INNER JOIN` — the foundation for reconnecting related data that was separated by good design. |
| [Part 6: Aggregation, Grouping, and Calculated Results](#part-6-aggregation-grouping-and-calculated-results) | How do you summarize patterns across many rows? | `GROUP BY`, `COUNT`, `AVG`, `SUM`, and `HAVING` transform raw records into actionable summaries. |

Work through the [Core Concepts](/book/ch05/core-concepts/1) for the full chapter, then apply your skills in the [Let's Build](/book/ch05/lets-build/1) section.
