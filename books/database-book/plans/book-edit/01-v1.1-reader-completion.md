# Phase 1: Reader Hybrid v1.1 Completion Pass

**Objective:** Finish the visual/motion/layout redesign so v1.1 matches the Google AI Studio aesthetic.
**Type:** Frontend-only. No backend, no deployment.

---

## Scope Boundaries

### Work only inside

```
books/database-book/platform-pilots/reader-hybrid-v1.1/
```

### Do NOT modify

- `reader-hybrid/`
- `reader-hybrid-alt/`
- `google-ai-studio/`
- `reader-hybrid-v2/`
- `files/source/`
- Any repo-level config outside `reader-hybrid-v1.1/`

### Do NOT add

Supabase, Firebase, Stripe, real auth, real payment, database logic, backend routes, API keys, real AI, AI Assistant navigation, LMS integration, note persistence, server-side code.

### Preserve

- React 18, Vite 5
- Build-time Markdown generator (`generateBookData.ts`)
- Generated data model (`BOOK_CHAPTERS`, `FLAT_READER_PAGES`, `BOOK_LABS`)
- Page-break reader model
- Query-param deep links
- **Core Concepts** naming (NEVER "Main Concepts")
- Frontend-only demo login
- Markdown sanitization (`react-markdown`, `remark-gfm`, `rehype-raw`, `rehype-sanitize`)
- H2/H3 heading extraction and "On this page" click navigation
- Mobile "On this page" details block
- `PAGE_INDEX_MAP`
- Scroll-to-top behavior
- v1.1 localStorage keys

---

## Step 1.1 — Add motion dependency

- Add exactly one dependency: `motion` (React 18 compatible)
- Import from: `motion/react`
- Use `useReducedMotion` from `motion/react`
- Update `package.json` and `package-lock.json`
- Do NOT add `framer-motion`, GSAP, Tailwind, React 19, Vite 6

## Step 1.2 — Create AnimatedBookCover component

- **File:** `src/components/AnimatedBookCover.tsx`
- **Image:** `https://res.cloudinary.com/dkndq6lyz/image/upload/q_auto,f_auto/cover-5-20_ajw15x.jpg`
- **Alt text:** "Using Data to Drive Business Performance book cover"
- **Loading:** eager (above the fold), `fetchPriority` high
- CSS perspective wrapper with book-spine/depth treatment
- **Entrance motion** (reduced-motion off): opacity 0→1, y 24→0, rotateY -8→-3deg, scale 0.96→1, ~0.7s
- **Hover** (pointer devices): slight lift, subtle tilt, scale ≤1.02, stronger shadow
- **Reduced motion:** static cover, no transforms, no tilt

## Step 1.3 — Redesign home page hero

Convert the current vertical stack to a wide two-column desktop hero.

**Desktop layout:**

```
┌─────────────────────────────────────────────────────────┐
│ Prototype pill           │                              │
│ Book title               │    Animated book cover       │
│ Subtitle                 │                              │
│ Author                   │                              │
│ Description              │                              │
│ CTA buttons              │                              │
└─────────────────────────────────────────────────────────┘
```

**Mobile:** text first, cover second, no overflow.

### Exact hero text

- **Pill:** "Frontend Prototype"
- **Title:** "Using Data to Drive Business Performance"
- **Subtitle:** "Databases, information systems, analytics, and managerial decision-making."
- **Author:** "Nimrod Dvir, PhD"

### Exact description

> Using Data to Drive Business Performance is an applied introduction to databases, information systems, analytics, and managerial decision-making. The book teaches students how data moves from raw records into structured tables, relationships, queries, dashboards, and business decisions. Through examples, labs, and review activities, students learn not only how database systems work, but why they matter for organizational performance.

### CTAs

1. **Enter Reader** (primary)
2. Open Labs
3. Demo Login / Access

### After hero

1. Feature cards
2. Course Overview Video (secondary)
3. Table of Contents
4. Prototype notice

### Feature cards

| Card | Message |
|---|---|
| Learn the structure | Data, tables, relationships, and SQL. |
| Practice the workflow | Labs, examples, and applied exercises. |
| Think like a manager | Analytics, systems, and decisions. |

Remove redundant lower Overview paragraph if it duplicates the description.

## Step 1.4 — Home page motion

- Staged entrance: hero text → description → CTAs → cover → feature cards
- Feature cards: opacity 0→1, y 12→0, small stagger, hover translateY ≤-3px
- Do NOT animate every TOC row
- Respect reduced motion globally

## Step 1.5 — Reader layout width cleanup

Remove the legacy `.chapter-reader { max-width: 780px; }` rule.

### Target widths

| Selector | Value |
|---|---|
| `.app-body` | width 100%, max-width ~1680px, centered |
| `.main-content` | width 100%, min-width 0, `padding: 2rem clamp(1.5rem, 3vw, 3.5rem) 4rem` |
| `.chapter-reader` | width 100%, max-width ~1380px, centered |
| `.reader-body` | grid, columns `minmax(0, 1fr) 240px`, gap ~2.5rem |
| `.markdown-body` | max-width ~900px |
| `.reader-content` | width 100%, min-width 0, subtle border/shadow |
| `.on-this-page` | sticky, subtle left border |

### Breakpoints

| Breakpoint | Behavior |
|---|---|
| ≥1440px | Right rail 260px, gap 3rem |
| <1280px | Hide desktop rail, show mobile OTP, one-column |
| <768px | Mobile drawer, reduced padding, horizontal scroll for tables/code |

## Step 1.6 — Reader page transition

- Wrap reader article area in motion, keyed by `page.id`
- opacity 0→1, y 8–12→0, duration 180–250ms
- No movement if reduced motion on
- Do NOT animate sidebar/header/bottom nav on every page change

## Step 1.7 — Active "On this page" highlighting

- IntersectionObserver on H2/H3 elements
- Active ID updates on scroll
- Active link gets `.otp-link.active` class (primary color, left border)
- No continuous URL hash updates while scrolling; click may update hash
- Clean up observers on page change
- **If unstable:** defer and keep click-based navigation — do not break the reader

## Step 1.8 — Favicon and site icons

**Source:** `G:\My Drive\...\BITM330-book-drive\.images\ch00-general\logo\optimized-icons\`

**Copy to:** `reader-hybrid-v1.1/public/`

| File | Required |
|---|---|
| `favicon.ico` | Yes |
| `apple-touch-icon.png` | Yes |
| `icon-192x192.png` | Yes |
| `icon-512x512.png` | Yes |

**Create:** `public/site.webmanifest`

```json
{
  "name": "Using Data to Drive Business Performance",
  "short_name": "DIMA Reader",
  "description": "An interactive textbook on databases, information systems, analytics, and managerial decision-making.",
  "icons": [
    { "src": "/icon-192x192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512x512.png", "sizes": "512x512", "type": "image/png" }
  ],
  "theme_color": "#4F46E5",
  "background_color": "#F4F5F7",
  "display": "standalone",
  "start_url": "/?scope=welcome"
}
```

**Update `index.html` `<head>`:**

```html
<title>Using Data to Drive Business Performance | DIMA Publishing</title>
<link rel="icon" href="/favicon.ico" sizes="any">
<link rel="icon" type="image/png" sizes="192x192" href="/icon-192x192.png">
<link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
<link rel="manifest" href="/site.webmanifest">
<meta name="theme-color" content="#4F46E5">
```

**If G: drive unavailable:** stop the icon copy step, report missing files, do not fabricate.

## Step 1.9 — CSS consolidation

- Use `src/styles.css` as canonical stylesheet
- Verify `index.css` is unused → remove it (only after build passes)
- Resolve duplicate selectors: `.chapter-reader`, `.reader-body`, `.markdown-body`, `.home-page`, `.home-hero`, `.app-body`, `.main-content`, `.on-this-page`
- Group CSS logically:
  1. tokens/reset
  2. shell/header
  3. sidebar/mobile navigation
  4. home page
  5. animated cover
  6. reader layout
  7. Markdown
  8. On this page
  9. labs/login
  10. accessibility/reduced motion
  11. responsive breakpoints

## Step 1.10 — Sidebar (polish only)

- Keep scopes: Home, Book, Labs, Access
- **NO AI Assistant**
- Reader-area icons:

| Reader area | Icon | Subtitle |
|---|---|---|
| Introduction | BookMarked | Hook & Core Alignment |
| Core Concepts | Layers | Theory & Core Frameworks |
| Let's Build | Terminal / Database | Hands-on Code Laboratory |
| Review Questions | HelpCircle | Self-explanation Exercises |
| Terms Treasury | Sparkles | Key Glossary & Core Definitions |
| RAT | Award | Verify Knowledge Retention |

- Polish sizing/spacing only; no sidebar search or icon rail in this pass

## Step 1.11 — Technical fixes

- Fix fragile chapter ID parsing: replace `section.id.split('-').slice(0, 2).join('-')` with `section.pages[0]?.chapterId`
- Keep `PAGE_INDEX_MAP`
- Keep scroll-to-top
- Keep dismissible prototype notice
- Keep separate v1.1 localStorage keys
- Keep YouTube allowlist

---

## Acceptance Criteria

### Home page

1. Desktop uses a wide two-column hero
2. Text/CTAs on the left, animated cover on the right
3. Cover is no longer stacked above the hero on desktop
4. Cover animation is visible but restrained
5. Hero contains exact agreed title, subtitle, author, description, and CTAs
6. Feature cards appear below the hero
7. Video is secondary
8. No redundant overview copy remains
9. Home width ~1180–1280px or fluid
10. Mobile stacks cleanly with no overflow

### Motion

11. `motion` installed and imported from `motion/react`
12. `AnimatedBookCover` exists
13. Feature cards use subtle entrance/hover motion
14. Reader page transitions are subtle
15. Reduced-motion users get static/minimal motion

### Reader

16. `.chapter-reader` is not capped at 780px
17. Reader has enough width for article plus right rail
18. Article text ~900px max width
19. Right rail visible on wide screens
20. Mobile On This Page works
21. Active OTP highlighting works or explicitly reported as deferred
22. No horizontal overflow

### Favicon

23. `public/favicon.ico` exists (if source available)
24. `public/apple-touch-icon.png` exists (if source available)
25. `public/icon-192x192.png` and `public/icon-512x512.png` exist
26. `public/site.webmanifest` exists
27. `index.html` references favicon, Apple touch icon, manifest, theme-color, and title

### Build

28. `npm install` completes
29. `npm run generate` passes
30. `npm run lint` passes
31. `npm run build` passes

### Scope

32. AI Assistant is not added
33. Core Concepts remains correctly named
34. No source Markdown changed
35. No generated data manually edited
36. No backend, Stripe, Supabase, Firebase, real auth, database, or real AI added

---

## Design Tokens

### Core surfaces

| Role | Hex |
|---|---|
| App background | `#F4F5F7` |
| Shell | `#F8FAFC` |
| Main workspace | `#FFFFFF` |
| Raised surface | `#FDFDFE` |
| Muted surface | `#F1F5F9` |

### Text

| Role | Hex |
|---|---|
| Main | `#18181B` |
| Secondary | `#52525B` |
| Muted | `#71717A` |

### Borders

| Role | Hex |
|---|---|
| Soft | `#E4E4E7` |
| Stronger | `#CBD5E1` |

### Primary

| Role | Hex |
|---|---|
| Primary | `#4F46E5` |
| Hover | `#4338CA` |
| Soft | `#EEF2FF` |
| Border | `#C7D2FE` |

### Brand accents

| Role | Hex |
|---|---|
| Navy | `#1A3B66` |
| Blue | `#2D4F6F` |
| Teal | `#4D7992` |
| Gold | `#D9B44F` |
| Soft gold | `#FFF7DD` |
| Glow | `#FFB84D` |

### Section accents

| Reader area | Text | Background | Border |
|---|---|---|---|
| Introduction | `#4F46E5` | `#EEF2FF` | `#C7D2FE` |
| Core Concepts | `#2563EB` | `#EFF6FF` | `#BFDBFE` |
| Let's Build | `#059669` | `#ECFDF5` | `#A7F3D0` |
| Review Questions | `#D97706` | `#FFFBEB` | `#FDE68A` |
| Terms Treasury | `#7C3AED` | `#F5F3FF` | `#DDD6FE` |
| RAT | `#0F766E` | `#F0FDFA` | `#99F6E4` |

### Design ratio

90% white/zinc — 8% indigo/navy/teal — 2% gold

---

## Build Commands

```bash
cd books/database-book/platform-pilots/reader-hybrid-v1.1/
npm install
npm run generate
npm run lint
npm run build
```

Do not deploy. Local verification only.
